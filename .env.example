# lana-dw-postgres Environment Configuration
# Copy to .env and edit with your values

# =============================================================================
# SOURCE DATABASE (lana-bank)
# =============================================================================

# Connection string to the lana-bank PostgreSQL database
# This is the source data that will be extracted
LANA_PG_CON=postgres://postgres:password@localhost:5432/lana

# =============================================================================
# DATA WAREHOUSE TARGET
# =============================================================================

# Target type: postgres (BigQuery not supported by this wrapper)
DW_TARGET=postgres

# =============================================================================
# DATA WAREHOUSE DATABASE
# =============================================================================

# Option A: Same database as lana-bank (recommended for dev)
# - Uses separate schemas within the same database
# - Set DW_PG_HOST to your lana-bank database host
# - Set DW_PG_DATABASE to 'lana' (same database)

# Option B: Separate database
# - Uses the dw-postgres Docker service
# - Set DW_PG_HOST to 'dw-postgres'
# - Set DW_PG_DATABASE to 'lana_dw'

DW_PG_HOST=localhost
DW_PG_PORT=5432
DW_PG_DATABASE=lana
DW_PG_USER=postgres
DW_PG_PASSWORD=password

# Schema names for data warehouse tables
DW_RAW_SCHEMA=raw
DW_DBT_SCHEMA=dbt

# =============================================================================
# DAGSTER CONFIGURATION
# =============================================================================

# Dagster home directory (used by Docker)
DAGSTER_HOME=/opt/dagster/dagster_home

# =============================================================================
# OPTIONAL: SEPARATE DW DATABASE (if using dw-postgres service)
# =============================================================================

# Password for the standalone DW postgres (docker-compose dw-postgres service)
DW_POSTGRES_PASSWORD=dwpassword
